{
  "name": "tebk-monorepo",
  "version": "1.0.0",
  "engines": { "node": ">=18" },
  "scripts": {
    "build:main":  "cd tebk-project && npm install && npm run build",
    "build:admin": "cd tebk-admin   && npm install && npm run build",
    "build:all":   "npm run build:main && npm run build:admin && node scripts/merge-dist.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0",
    "cloudinary": "^2.10.0",
    "dotenv": "^17.4.2"
  }
}
